# AR1 Implementation Root

This directory will contain AR1 source, tests, schemas, manifests, tools,
deliveries, and sanitized evidence. AR1M0 has not acquired models or created
real adapters. Legacy code remains available at immutable tag `audio_m4` and
must not be imported directly.
