"""Explicit milestone-suite entrypoints.

The modules in this package are callable historical/gate entrypoints.  They do
not re-export tests during collection; each entrypoint owns its explicit node
manifest or target suite.
"""
