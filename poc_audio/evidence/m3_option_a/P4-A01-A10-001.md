# P4 Option A partial validation evidence — A01 and A10

- **Test packet:** `M1-P4-OPTION-A-001`
- **POC SHA:** `cc366b826de755bbf422734647b452f77eb9b34d`
- **Execution date:** 2026-08-13 UTC
- **Environment pre-test:** `PASS`
- **Scope:** P4-A01 direct native open and P4-A10 build/license only
- **Overall packet:** `IN_PROGRESS`; P4-A02 through P4-A09 remain `Pending`

## Reviewed results

| ID | Result | Evidence summary |
| --- | --- | --- |
| P4-A01 | `PASS` | Direct `hw:0,0` capture realized `RW_INTERLEAVED`, 48 kHz, 2 channels, `S32_LE`, period 960, buffer 3840; close completed and ALSA owners returned to zero. |
| P4-A10 | `PASS` | Both pinned sdists built on Raspberry Pi 5/aarch64; a second clean build used package indexes disabled and only hashed local inputs, then installed and imported successfully. |

The first A01 probe is retained as `INCONCLUSIVE`: it called getters absent from
pyalsaaudio 0.11.0 and a pipeline without `pipefail` masked the Python exit. The
corrected probe reads realized parameters from the kernel ALSA `hw_params` while
the stream is open and propagates every failure.

The first generic `pip download --no-binary=:all:` attempt is also retained as a
reproducible failure. It recursively attempted to source-build CMake and failed
without OpenSSL development headers. Candidate sdists were subsequently fetched
from their pinned official URLs, verified before build, and built with the
system CMake plus local hashed external sources.

## Target identity

- Raspberry Pi 5 Model B Rev 1.1, aarch64
- Debian GNU/Linux 13, kernel `6.12.47+rpt-rpi-2712`
- Python 3.13.5, GCC/G++ 14.2.0, CMake 3.31.6
- ALSA 1.2.14 and `libasound2-dev 1.2.14-1+rpt1`
- pyalsaaudio 0.11.0 dynamically links `libasound.so.2`
- samplerate 0.2.4 statically contains the pinned libsamplerate source

## Cleanup and evidence boundary

After build, import, direct open, failed probes, and corrected probes:

- candidate build processes: 0
- candidate Python processes: 0
- candidate threads and open file descriptors: 0
- ALSA device owners: 0
- Pi Git worktree: clean at the exact SHA

Raw logs, built wheels, source archives, dependency artifacts, native linkage,
and target-specific paths remain in the controlled Git-ignored packet directory.
Only hashes and this reviewed sanitized summary enter Git. No raw PCM was read or
retained by A01.
