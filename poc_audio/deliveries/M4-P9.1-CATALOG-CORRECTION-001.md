# M4-P9.1-CATALOG-CORRECTION-001

Status: `USER APPROVED / SUPERSEDES ONE CATALOG ROW`

## Decision

The User approved this correction on 2026-08-25. `M4-SESSION-08` replaces
`asr-pause-037` with `asr-pause-038`. The session ID, `code_switch` category,
pause class, `tts-008` mapping and the other 19 catalog rows remain unchanged.

## Pre-existing evidence basis

This corrects a known entry error, not post-result tuning. The corrected M2
Silero evidence existed before the M4 catalog was frozen:

- controlled result SHA-256:
  `93ca6d3d91842922240a84450145fc949bee69850d0003e37608f705fb0225a4`;
- `asr-pause-037`: zero candidate events, zero capture intervals and reviewed
  disposition `Confirmed hard low-volume fixture failure`;
- `asr-pause-038`: two candidate events merged by the frozen 500/600 ms capture
  padding into exactly one `[620, 5240] ms` interval, with complete utterance
  coverage.

The replacement rule is deterministic: use the next recording-plan fixture in
the same `code_switch`/`pause` class. No model, threshold, padding, runtime,
candidate, gate or remaining fixture is changed.

## Rejected-run retention

Candidate `b7b25ffcd964602531ff1f86ecaa218169f18972` stopped at session 08 with
`NO_SINGLE_BOUNDED_UTTERANCE`. Its draft formal evidence remains at
`controlled://audio-poc/m4/20260825/p9.1-b7b25ff`, raw SHA-256
`338fbd5b3ac7722742db001670f132153548af7870f31e1d72201705e271c640`.
Sessions 01–07 proved the P9.1 sequence but provide no partial PASS credit.

A new packet hash, candidate SHA, fixture lock and exact-SHA User authorization
are required. P9.1 restarts from session 01; it does not resume at session 08.
