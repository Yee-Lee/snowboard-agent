# M4-P9.1-SAMPLER-TIMESTAMP-001

Status: `DRAFT FAIL / METHOD CORRECTION REQUIRED`

Candidate `ffcfaa85c9db98333b5ec879f22515bf870b19d1` completed all 20 P9.1
sessions with zero cleanup. Across 888 resource records, peak used memory was
`3330.422 MiB`, swap remained zero and throttling remained `throttled=0x0`.
One reported interval was `0.532864 s`; the median was `0.249929 s`.

The timestamp was recorded after synchronous `/proc` collection, so the gap
mixed variable RSS/PSS collection cost into the scheduled sampling interval.
The append-only correction records the timestamp immediately before collection
and separately records collection duration. It retains the `0.25 s` schedule
and the stricter `0.5 s` continuity gate; no threshold is relaxed. Draft evidence
remains at `controlled://audio-poc/m4/20260825/p9.1-ffcfaa8` with SHA-256
`fb10f0b6022af020c6d91b3f6c2a833a2f434bf8405fdc9788ee36cb8c8aee88`.
