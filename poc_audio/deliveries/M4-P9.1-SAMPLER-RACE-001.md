# M4-P9.1-SAMPLER-RACE-001

Status: `REJECTED EVIDENCE / FIX REQUIRED`

Candidate `d36490f62679f50a3c109c4a10e80f7ee45221ad` completed all 20 corrected
P9.1 sessions with zero cleanup, peak used memory `3330.422 MiB` and no observed
throttle. The draft result must not be published as PASS: its background sampler
stopped after 340 records when a completed transient P9 PID disappeared between
the `/proc` existence check and `smaps_rollup` read. The runner incorrectly
reported a fixed `0.5 s` interval instead of validating actual timestamp gaps.

The append-only correction catches the benign per-PID `ProcessLookupError`,
records any other sampler-thread exception, fails the formal run if that thread
is unhealthy, samples on a monotonic `0.25 s` schedule, and rejects any observed
gap over `0.5 s`. The rejected controlled evidence remains at
`controlled://audio-poc/m4/20260825/p9.1-d36490f`; no partial PASS credit is used.
