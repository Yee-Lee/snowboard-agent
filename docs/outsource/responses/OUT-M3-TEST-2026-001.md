# Response: OUT-M3-TEST-2026-001

* **Handoff ID**: `PM-OUT-260806-004-m3-target-device-test-ack`
* **Feedback ID**: `OUT-M3-TEST-2026-001`
* **Status**: `ACK Confirmed`
* **Comparison baseline**: `7bc694d3edd2d06657c1081cb5f12a4dae13b554`
* **Delivery commit SHA**: TBD（提交後更新）
* **Architecture change**: `No`

---

## 六點逐項 ACK

**1. M3 依 Hardware readiness、Audio、Display、Camera、GPIO、integration/fallback 分段驗證，不以一次大型人工 smoke 取代分段結果。**

✅ ACK。M3 驗收採分段進行，各硬體項目各自有獨立測試節點與證據。`tests/milestones/test_m3_rpi_hal.py` 設計上亦依此原則分區，確保分段可重現、分段可追溯。

---

**2. 每個需操作實體 Pi 的項目使用有 ID 的 test card，至少列前置硬體 / 接線、被測完整 SHA、config hash、命令、使用者動作、預期結果與回傳產物。**

✅ ACK。M3 test spec 制定時，每個需要人工操作的項目（喇叭聲音確認、OLED 可讀性確認、按鍵 / 接線 / 插拔等）均以具 ID 的 test card 格式記錄，欄位包含前置條件、被測 SHA、config hash、完整命令序列、使用者動作步驟、預期結果與需回傳的產物清單。

---

**3. 測試工具自動保存 Pi 型號、OS/kernel、Python/dependency、device discovery、config hash、命令、時間、exit code、log 與 machine-readable result；人工結果不取代可自動驗證的契約。**

✅ ACK。M3 rpi marker 測試的 evidence 目錄將包含自動收集的系統環境快照（型號、kernel 版本、Python 版本、pip freeze、device discovery 輸出、config hash）以及每個命令的時間戳、exit code 與 machine-readable 結果摘要（JSON / text）。可自動驗證的行為（buffer 呼叫順序、exit code、log 內容）以自動化斷言為準，不被人工 checklist 覆蓋。

---

**4. 使用者只對喇叭聲音、OLED 可讀性、接線 / 按鍵 / 插拔等不可自動完成項目回覆 checklist；聊天或口頭的「看起來正常」不作 acceptance evidence。**

✅ ACK。人工 checklist 只涵蓋不可自動化的感知項目（聽覺、視覺、物理操作），並以結構化 checklist 回傳（含硬體型號、config hash、測試時間、pass / fail 及備註）。口頭確認不計入 acceptance evidence。

---

**5. 外包 Pi 自驗與內部指定 Pi 的獨立 confirmation 是兩層證據；外包自驗不能取代內部 Tester 對 delivery exact SHA 的正式硬體驗收。**

✅ ACK。外包 Pi 自驗（開發 / 測試用 Pi）與內部指定 Pi 的 Tester 獨立驗收為兩個分開的 evidence 層，各自記錄不同硬體資訊與 SHA。外包 delivery 只能提供自驗層，正式 M3 Accepted 必須由內部 Tester 對 delivery exact SHA 獨立執行後才能成立。

---

**6. 未來 M3 delivery 的大型資訊、影像或影片可落在受控位置，但 repo 內必須提交版本 / checksum、metadata、結果摘要與可定位索引，且不得含 credential 或不必要的個資內容。**

✅ ACK。大型 evidence 檔案（影像、影片、長篇 log）可放置於受控外部位置，repo 內 `docs/outsource/evidence/` 只提交：版本標記、SHA-256 checksum、metadata（產生時間、設備、測試命令）、結果摘要與可定位索引（URL / path）。所有 evidence 在提交前確認不含 credential、密碼、個人路徑或不必要個資。

---

## 尚待內部決策

- **M3 test spec 的具體命令、門檻與完整 test card 格式**：將於 M3 Design Gate（`OUT-M3-DESIGN-2026-001`）完成後，由 Tester 依 test spec 制定；本 ACK 不預先固定這些細節。
- **外部 evidence 儲存位置**：具體受控位置（如私有 storage bucket 或 NAS）由 PM / 內部團隊決定後告知；本輪 ACK 僅確認原則。
