# M4A Tester final sign-off — candidate 6c3ba954

## Identity and disposition

| Field | Value |
| :--- | :--- |
| Candidate SHA | `6c3ba95455dc5c2a152aa230b8ae5915887fe6a9` |
| Tester run ID | `m4a-6c3ba954-20260829-t01` |
| Portable matrix | **Pass** |
| Designer candidate freeze | Confirmed for exact candidate |
| Pi run ID | `m4a-6c3ba954-20260829-pi01` |
| Pi boot ID | `a0dc71ef-3254-4606-b7a8-eeab5421220e` |
| Target acceptance | **Pass** |
| Tester disposition | **M4A technical sub-gate Pass** |

Raw portable logs remain Git-external under
`<tester-run-root>/m4a-tester-evidence-6c3ba954-20260829-t01/portable/`.
Tester created a clean detached worktree at the externally supplied SHA and did
not reuse Developer output or run identity.

## Candidate scope audit

The candidate is an append-only descendant of rejected SHA `7aba0719…`. Its
protected delta is limited to the production telemetry invariant in the parent,
ASR supervisor and TTS worker plus the directly required offline regression
tests. The candidate still retains the full-tree network syscall audit; it does
not filter DNS, destinations, failed connections or network namespaces.

## Formal portable matrix

| Runtime | Result | Counts |
| :--- | :--- | :--- |
| CPython 3.11.16 | Pass | 171 passed; 0 failed/skipped/xfailed |
| CPython 3.12.3 | Pass | 171 passed; 0 failed/skipped/xfailed |
| CPython 3.13.15 | Pass | 171 passed; 0 failed/skipped/xfailed |

Each `candidate_gate.py portable` execution used the tracked
`tests/m4a_portable_suite.txt`, marker `not rpi`, a 120-second hard timeout,
disabled third-party pytest entry-point autoload and an empty inherited
`PYTHONPATH`. The generated `matrix-index.json` has status `Pass`; every result
binds candidate `6c3ba95455dc5c2a152aa230b8ae5915887fe6a9` and run
`m4a-6c3ba954-20260829-t01`. Protected paths remained clean afterward.

## Rebooted Pi target acceptance

After Designer froze the exact candidate, Tester rebooted the Pi and verified a
zero-process, zero-ALSA-holder and zero-temp baseline on the new boot. To avoid
repeating an already completed build, the run reused only Developer's
checksum-verified exact-SHA product and controller; Tester created fresh product
and target preflights, a new network namespace, a new run root and a new run ID.
The formal command deliberately inherited `ORT_DISABLE_TELEMETRY=0`, requiring
the candidate's production boundary to override the conflicting value.

| Check | Result |
| :--- | :--- |
| Product preflight | Pass; exact candidate, product schema v4 and locked artifacts |
| Target preflight | Pass; CPython 3.13.5 and same portable run identity |
| Full real-device suite | 7 passed; 0 failed/skipped/xfailed in 173.109 seconds |
| Full descendant network trace | 0 IPv4/IPv6 syscalls; downloader calls 0 |
| ASR | Real transcript, persistent two-turn and Level 2 recovery Pass |
| TTS / HAL | Real 16 kHz mono S16_LE synthesis, ALSA drain and recovery Pass |
| Resource | 20 turns; p99 7724.653 ms; peak used 829.703 MiB; `throttled=0x0` |
| Cleanup | All process/thread/fd/temp counters 0; final baseline clean |
| Privacy / packaging | 0 privacy hits; 8 locked wheels; notices Pass |

The authoritative sanitized target summary is `m4a/target/result.json`; the nine
signed Test-ID cards are under `m4a/target/cards/`. Full raw stdout, stderr,
JUnit and syscall trace remain Git-external at `<pi-run-root>/acceptance/`.

## Inheritance and final disposition

`m4a/inheritance.json` was generated by `scripts/m4a_inheritance.py` only after
target acceptance. All 16 required areas resolve to the immutable Accepted Audio
delivery, its verified SHA-256, this product SHA and a matching passing Core
result card. The original `TRDEV-M4A-005` false-green path is closed by the
formal full-tree zero-attempt result, not by the earlier short diagnostic.

Tester resolves the Developer review and records the M4A technical sub-gate as
**Pass** for candidate `6c3ba95455dc5c2a152aa230b8ae5915887fe6a9`.
Per the milestone boundary, the shared real M4A+M4B combined resource row remains
Pending until M4B input is Accepted; it is not represented by an Audio POC
surrogate. Overall M4 completion and `core_m4` tagging remain outside this M4A
Tester sign-off.
