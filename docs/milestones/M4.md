# M4 規劃

## 6. M4 ── 本機 AI 語音主線

M4 是單一 milestone，但包含三個可追溯的子 gate：M4a Audio、M4b LLM、M4c Session Display。M4a 與 M4b 可在各自輸入 Accepted 後並行準備；M4c 必須等 M4a、M4b 都通過，才能以真實 session 資料完成顯示接線。只有三個子 gate 都在同一產品 delivery exact SHA 通過，M4 才可標記 Accepted。

### 6.1 範圍

#### M4a ── Local Audio

* 實作真實 ASR 與 TTS adapter，格式與 M3 已驗收的 Audio HAL config 一致。
* 固定 WAV fixture 可產生非空 ASR result；固定文字可產生格式正確的 TTS PCM 並完成播放。
* 固定 ASR / TTS engine、model、voice、版本、授權、checksum、Pi 安裝方式及 benchmark evidence。
* Audio POC M2A以低成本packet建立ASR全景baseline；M2B只對shortlist作DSP / endpoint、decoder/runtime、number/domain與post-processing單變因優化。兩階段的quality / performance metric只作scorecard，不是hard gate。
* M2B provisional selection只放行candidate-specific adapter與provisional dependency/config integration；Audio M3 target/HAL qualification後才可準備產品exact-SHA acceptance。Gate 2B `POC Accepted` final handoff固定reference package / conformance kit後，才可鎖定production dependency / model / DSP / decoder profile / voice。
* Core Gate 3逐項記錄POC evidence inheritance與product-delta；候選比較不無理由重跑，但adapter / HAL wiring、config / lock / packaging、RM / SM lifecycle、composition、resource、offline及產品SHA regression必須重驗。

#### M4b ── Local LLM

* 實作 LiteRT-LM persistent child process；Engine load後必須經固定非敏感pre-warm與disposable Conversation cleanup才發布`INFERENCE_READY`（wire可仍名READY），再接受單 turn Conversation、result / timeout / shutdown。
* `docs/protocol.md`固定child protocol version、READY、structured generate、single result、cooperative cancel、error與shutdown wire schema；不得傳raw prompt或增加parent-visible stream chunk。
* 實作 child `force_abort()` 的 terminate / kill / waitpid completion proof，以及 Resource Manager rebuild / recovery barrier。
* 固定 LiteRT-LM artifact、版本、授權、checksum、Pi 安裝方式及 benchmark evidence。
* POC輸出必須先經Core product validator正規化為`LLMResponse(action_kind, action_payload, next_perceptions)`；模型只產`speak/tool/rest` intent，不執行tool handler。空輸出、拒答、壞JSON與unknown action走P5 apology-speak或rest fallback，prompt / raw output / payload不得進log。

#### M4c ── Session Display

* 完成 Button → Listen → ASR → Reasoner → LiteRT-LM → Speak → TTS → Rest 的 Raspberry Pi 5 離線主線。
* 依 `display_spec.md` 將已驗證的 state、Perception、Tool、Speak、Interrupt 與 sanitized Error 投影至 M3 已驗收的 Display HAL、Renderer / Arbiter。
* Tool 畫面只驗證 sanitized display intent / fixture；實際 ToolRegistry handler 與外部派發仍屬 M5，不因 M4c 提前實作。
* 實作 `SET-SHOW-SESSION-CONTENT` 的 startup config mapping；關閉後不得改變 session、action、audio、resource lifecycle 或 exit code。
* Startup / shutdown 維持 Fullscreen Blank；不加入 Progress、動畫、正式 icons 或完整對話歷史。
* SSD1351正常stop在native handle有效時best-effort present一個全零RGB565 full frame後才close；present失敗仍須完成handle / GPIO / SPI cleanup，重複stop冪等。Acceptance runner在正常完成、任一卡失敗或中斷時都必須執行final Display shutdown。

### 6.2 相依與放行規則

| Gate | 相依 | 放行條件 |
| :--- | :--- | :--- |
| M4 common entry | M3 Accepted | 引用 M3 Accepted exact SHA；不得以部分 M3 feature 或 POC branch HEAD 取代 |
| M4a Audio | Accepted Audio POC final reference | **Core Gate 3 Accepted**：Audio POC baseline為`audio_m4` / `5694ead4ba6be928fdb4dbdf6da7155b214d72bd`；Core product candidate `6c3ba95455dc5c2a152aa230b8ae5915887fe6a9`完成3.11／3.12／3.13 portable matrix、rebooted Pi ASR/TTS/HAL、zero-attempt offline、cleanup、packaging與16-row inheritance。Designer final record為`CR_M4_II`，evidence為`M4A-TESTER-6C3BA954-20260829`。共享real M4a+M4b resource row仍等待M4b，不回退M4a Accepted。 |
| M4b LLM | Accepted LLM POC winner | **Gemma POC winner Accepted；Core Gate 3 Pending**：Gate 2B execution SHA `0c75536e6ee99b502c59438989ca852194648946`、surface `22f52d8…`之Attempt 006完成20/20 full-chain session，但frozen combined PSS gate超標，machine P9/P10B維持`FAIL`；User以`KNOWN_RUNTIME_DEFECT / ENGINE-SESSION RESIDENT RETENTION` waiver選定Gemma。Core已驗證docs-only closure `5ffdd9e…`與publication `485bb2a…`的ancestry、diff scope及enclosure digests，並由`DELIVERY-LLM-POC-M4B-GATE2B-FINAL-WINNER-ACK-001`接受R3為產品設計input。此waiver不授予Core product PASS；persistent-child implementation須等待protocol design review與Tester test-spec coverage review，並在Gate 3對產品exact SHA重驗bounded recycle、4 GB offline 20-session resource envelope、cleanup與recovery。 |
| M4c Session Display | M4a + M4b 通過 | `display_spec.md` 的 Session scenarios 與 privacy mapping 已納入 test spec；Display POC 若提出不同產品內容，Designer 以 `display_spec.md` 為準拒絕或另開產品決策，並記錄於 `milestone_progress.md`。單純提案不阻擋 M4c；未解的技術契約矛盾仍依 `milestone.md` §1.2 阻擋 |
| M4 candidate freeze | M4a + M4b + M4c implementation complete | 依 `docs/runbooks/candidate_hardware_gate.md`：USER核准provisional candidate commit後，CPython 3.11 / 3.12 / 3.13對該SHA執行portable matrix並達0 Fail / Blocked / Skip / XFail；Designer candidate review無Blocking後，凍結同一完整SHA |
| M4 Accepted | frozen candidate | Pi 3.13 preflight與一次完整 RPI-NATIVE acceptance 通過；Tester 確認 portable matrix與Pi result使用同一產品 delivery exact SHA，正式target result未混用run ID |
| ALPHA entry | M4 Accepted | M4 Accepted 是 ALPHA Gate 的前置條件；M4 通過不推定 ALPHA 通過。ALPHA Design Ready 後，Tester 建立 `test_spec_ALPHA.md`，Designer 簽核後方可開始 ALPHA 開發。詳見 `docs/milestones/ALPHA.md`。|
| M5 entry | M5 baseline selection complete | M5 必須引用唯一被選定的 `ALPHA Accepted exact SHA` 或 `ALPHA.R1 Accepted exact SHA`；不得引用 M4 / M4.R1 部分結論、同時引用兩個 baseline，或拼接不同 SHA evidence。見 `docs/milestones/ALPHA_R1.md`。|

#### 6.2.1 M4a / M4b POC convergence without circular dependency

1. Audio先完成M2A baseline與M2B optimization，Core依comparative scorecard作provisional selection；此時只放行candidate-specific adapter與provisional dependency/config integration。Audio M3再完成target/HAL qualification。LLM可平行完成自己的Gate 2A，兩邊不互相等待。
2. Audio POC以Core核准的deterministic M4b resource surrogate完成internal M4，經review成為`POC Accepted` final reference package；不等待LLM final winner，因此不形成循環依賴。
3. LLM Gate 2B再引用該Accepted Audio package執行P9 residency與P10B 20-session combined gate，產生LLM final winner ACK。
4. Audio final input已固定於`model_spec.md`，可先行實作M4a product adapter；Core可在LLM Gate 2A前完成`docs/protocol.md` engine-agnostic Phase A review，兩邊final input齊備後再審selected READY identity與真實composition candidate。POC evidence只作immutable reference，Core Tester仍驗產品exact SHA。
5. Audio M2A/M2B不以quality / performance threshold淘汰ASR candidate；artifact / license / offline / bounded execution / cleanup仍須使觀察有效。任一Gate 2B發現resource、offline、lifecycle或license Blocking時，停止受影響baseline lock / acceptance並走change request；未受影響的protocol / fake scaffold可繼續，但不得宣稱Accepted。

Accepted Audio與LLM POC packages均完成Core intake，且Developer建立Core-owned composition
smoke command後，Core Tester可先以`scripts/m4_memory_preflight.py`包住該命令，再決定是否投入
昂貴的integration repetition、quality與soak。
這是可隨時重跑的測項`M4-REG-001`，不是新Gate：POC不負責combined執行，結果預設只輸出
stdout、不要求保存baseline / PASS evidence，也不改變Gate 2A / 2B狀態。正式resource結論仍由
M4A-P9、M4B-P9 / P10B及Core產品acceptance作成。

#### 6.2.2 M4b production planning slices

| Slice | Entry | Designer / downstream output | 明確禁止 |
| :--- | :--- | :--- | :--- |
| B0 — generic Designer Phase A | Gate 1 closed | `implement/ch_m4b_llm_production.md` generic design、`protocol.md` §6、exact protocol/fake seam與intake contract完成 | 依USER single-review方向先不開generic-only IR/TR，不宣告Design Ready |
| B1 — Gate 2A closure / integration qualification | Gate 2A User review + Core ACK | Gemma sole model finalist；依`DELIVERY-019`最多兩個bounded development revisions，freeze後以separate catalog重驗affected P2/P8；可規劃不鎖production identity的LiteRT-LM scaffold | 不改寫R1 FAIL、不把scored cases寫進prompt、不以normalizer/retry/threshold relaxation製造PASS |
| B2 — baseline closure | New Gemma revision P2/P8 PASS；replacement packet accepted；Gate 2B以Accepted Audio完成P9/P10B；Core final winner ACK | 固定`model_spec.md`、selected driver/config/lock/notice；runtime若偏離LiteRT-LM先解`AR_impl` | 不以surrogate、branch HEAD、POC自驗或不同candidate evidence拼接final input |
| B3 — single design/test gate + Core Gate 3 | Gate 2B final ACK | **Implementation in progress；target ABI spec delta pending**。`IR_review_M4B_I`與`TR_spec_M4B_I`均Resolved；Developer的`IR_dev_M4B_I`確認原self-contained CPython closure缺少authority，Designer已採target ABI boundary並開`TR_spec_M4B_II`局部修訂LOCK/PKG assertions。Developer可繼續portable實作，但WP-02／04／06 target-ready、provisional candidate與Pi Gate 3須等`IR_dev_M4B_I`與`TR_spec_M4B_II`均Resolved。Product recycle固定為每child最多8次attempt，或post-prewarm owner PSS增量`>=48 MiB`，或target `MemAvailable <768 MiB`；terminal cleanup後排程同key RM recovery，replacement `INFERENCE_READY`前barrier保持關閉，無request waiter時仍由main的RM fatal monitor監督。20-session完整資料保留r14 4/64 gates | 不拆成generic/selected多輪；不在active request中recycle；不以缺失resource sample宣告PASS；不因recycle分段重算／刪sample；不以POC PASS取代Core product PASS；不拼接M4a舊SHA與M4b-only SHA宣告整體M4 |

### 6.3 排除項目

* 不讓 LiteRT-LM 自動執行 Python tool handler；模型只回傳 tool intent。
* 不保留跨 turn / 跨 session 隱藏 conversation history。
* 不實作雲端 LLM、query action 或完整 reasoning loop。
* 不把 native blocking inference 放在主 process thread。
* 不要求 MQTT、voice wake 或 Vision 路徑完成。
* 不加入 Progress UI、Display 動畫、正式 icons 或 pixel-level polish。

### 6.4 可重複驗收

1. 候選驗收順序不可調換；完整操作及 runner contract 見 `docs/runbooks/candidate_hardware_gate.md`：

```bash
# Developer fast loop（團隊主要版本）
python -m pytest -v <affected-nodes>

# Candidate portable gate（CI / 集中環境，三版本執行同一 suite）
python3.11 -m pytest -v -m "not rpi" <M4-portable-suite>
python3.12 -m pytest -v -m "not rpi" <M4-portable-suite>
python3.13 -m pytest -v -m "not rpi" <M4-portable-suite>

# 完整 SHA freeze 且 Pi preflight PASS 後，Pi 3.13 只執行一次正式 gate
python3.13 -m pytest -v -m rpi tests/milestones/test_m4_local_voice.py
```

上列是 test-spec 定稿前的命令形狀，不授權 runner 從目前 HEAD 推導 candidate。正式命令由 candidate gate runner加入bounded timeout、外部SHA / run ID與新output；failure保存result及raw log。Debug可按需要執行，但其結果不得標記或合併為正式Pass。

在上述正式Pi gate之前，`M4-REG-001`只包住完成兩份Accepted POC intake後的Core-owned composition
smoke command作即時capacity診斷；此處的memory preflight與candidate runner的identity preflight
不同，不要求candidate SHA / run ID，也不產生正式PASS card。命令及判定見
`docs/test_spec/test_spec_M4.md`。

2. M4a 至少證明：

* 固定 WAV fixture 可產生非空 ASR result；固定文字可產生格式正確的 TTS PCM 並完成播放。
* AudioInput delivered stream / ASR格式一致，TTS / AudioOutput stream格式另行一致；除M3核准的AudioInput HAL native→stream adaptation外，Listen / ASR / TTS / Speak不在runtime隱式resample。
* M4a 與 M4b 同時常駐時仍符合 target-device resource budget。
* Gate 3 evidence index逐項列出Accepted Audio POC SHA / kit revision、繼承理由及產品delta Test ID；不得只寫「沿用POC」。

3. M4b 至少證明：

* LiteRT-LM `start()` 只在Engine load、mandatory pre-warm、disposable Conversation close與state cleanup完成後的`INFERENCE_READY` ACK才return；連續兩turn不重載engine，也不沿用隱藏Conversation history。Engine/process recycle後同樣重走pre-warm與recovery barrier。
* 模型 timeout、拒答或壞 JSON 在 clean stop 時走 P5 fallback。
* Cooperative cancel 失敗時 Level 2 終止並 waitpid child；recovery barrier 清除前 SM 不回 IDLE、不接受新 wake。
* Rebuild 失敗 / timeout 進 Level 3、exit 4；測試結束沒有 orphan child。
* 8-attempt / 48 MiB owner-PSS / 768 MiB `MemAvailable`任一門檻觸發planned recycle；20-session combined run至少觀察兩次replacement generation，且每次都保留exact lock、重跑pre-warm並由RecoveryTicket阻擋下一次admission。缺少target sampler資料不得宣告PASS。
* 20-session完整samples仍以r14公式驗combined PSS與system-used各自slope`<=4 MiB/session`、late-minus-early median delta`<=64 MiB`；每generation owner-PSS baseline-to-terminal delta亦`<=64 MiB`。不得因recycle分段重算、刪sample或把48 MiB trigger當成放寬gate。
* 固定catalog涵蓋`speak/tool/rest`、capability allowlist、空輸出、拒答、壞JSON、unknown action / tool；正常schema 100%通過，失敗輸出走P5且log不含prompt、raw output或payload。
* Native cooperative cancel不可用但Level 2 terminate→kill→waitpid→rebuild / READY barrier成功時，候選仍可eligible；Level 2或rebuild失敗才進Level 3。

4. M4c 至少證明：

* Display 只呈現目前 session / turn 的已驗證內容；不顯示 credential、prompt、hidden context、raw tool arguments 或 raw model output。
* `SET-SHOW-SESSION-CONTENT=false` 時隱藏 Perception / Tool / Speak，但 State、Error 與 Blank 仍依規格運作。
* Fullscreen ownership、Error clear、Interrupt clear 與回 IDLE 行為符合 `display_spec.md`；Display failure 不阻斷語音主線或改變 exit code。
* Host bridge證明SSD1351 stop成功路徑恰好present一次最終全黑frame後close，failure路徑仍close / release；Pi shutdown observation證明正常完成、測項失敗與中斷後OLED均保持全黑。
* 整條離線語音 session 不需網路，且 log 不含 transcript、prompt 或 raw model output。

5. M4 milestone conclusion 必須列出 M4a、M4b、M4c 各自 result locator，並共同引用同一被測產品 SHA；另引用三版本 portable matrix index與Pi preflight。正式target result使用單一acceptance run ID，不得把debug結果列為Pass。

---
