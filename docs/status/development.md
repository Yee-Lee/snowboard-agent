# Current development status

- Owner: Developer
- Scope: M4B MVA product delta
- Entry: **Closed**
- Active work package: none

Developer must not continue the previous M4B R1 candidate or implement the prospective MVA design while
the current handoff lists unresolved adoption/review/test-spec gates.

## On entry release

1. Read `docs/status/current.md` and the newly approved work-package scope.
2. Read only the cited sections of `docs/milestones/M4B_MVA.md`,
   `docs/implement/ch_m4b_llm_production.md`, the adopted 002 decision and current
   `docs/test_spec/test_spec_M4B.md`.
3. Search source/tests by affected symbol and Test ID; do not preload M4A or R1 history.
4. Record affected paths, Test IDs, estimate and verification commands here before editing.
5. After execution, replace this active summary with results and remaining blockers; move completed
   milestone snapshots to archive instead of appending a diary.

Prior M3/M4 progress and candidate checkpoints are preserved under `docs/status/archive/` and are read
only when a current finding names their ID or SHA.
